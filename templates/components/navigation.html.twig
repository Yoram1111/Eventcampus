<nav class="navbar navbar-expand-md border-bottom mb-4">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_accueil') }}">EventCampus</a>
        <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#menu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="menu">
            <div class="navbar-nav">
                <a class="nav-link" href="{{ path('app_evenement_index') }}">Événements</a>
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">Catégories</a>
                    <div class="dropdown-menu">
                        {% for cat in ['culturel', 'sportif', 'associatif', 'festif'] %}
                            <a class="dropdown-item" href="{{ path('app_evenement_categorie', {categorie: cat}) }}">{{ cat|capitalize }}</a>
                        {% endfor %}
                    </div>
                </div>
                <a class="nav-link" href="{{ path('app_evenement_filtre') }}">Filtrer</a>
                <a class="nav-link" href="{{ path('app_statistiques') }}">Statistiques</a>
            </div>
        </div>
    </div>
</nav>
