{% extends 'layout/main.html.twig' %}

{% block title %}Accueil - EventCampus{% endblock %}

{% block content %}
    <h1>Bienvenue sur EventCampus</h1>
    <p>{{ nb_evenements }} événements, dont {{ nb_a_venir }} à venir.</p>

    <h2>Prochains événements</h2>
    <div class="row">
        {% for evenement in prochains %}
            <div class="col-md-4 mb-4">{% include 'components/evenement_card.html.twig' %}</div>
        {% else %}
            <p>Aucun événement à venir.</p>
        {% endfor %}
    </div>
{% endblock %}
